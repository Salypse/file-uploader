<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= currentFolder ? currentFolder.name : "Root"%></title>
</head>
<body>
    <main>
        <% if (locals.content.length) {%>
            <%- include("./partials/shareBrowser") %>
        <% } %>
    </main>
</body>
</html>